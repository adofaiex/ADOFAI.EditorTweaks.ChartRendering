# 更新日志

本文档记录 ADOFAI Editor Tweaks 的用户可见改动。版本号来自 `Info.json`。

## 1.4.7

### 优化

- 优化 Web 设置页滚动布局，垂直滚动只作用于右侧内容，左侧模块导航保持固定。
- 移除页面左下角重复的快捷键提示，修正底部入口名称和跳转目标。

### 修复

- 修复镜头和镜头比例装饰修改轴心偏移后再次拖动出现位置漂移的问题。
- 修复镜头装饰视差刷新后轴心十字与装饰显示位置错位的问题。

## 1.4.6

### 优化

- 渲染进行时锁定所有会影响渲染结果的设置，完成后自动恢复可编辑。
- 渲染完成、失败或取消后立即恢复正确阶段，并在页面顶部显示明确结果和输出路径。
- 手册和 FFmpeg 参数参考改为由本地 Web 服务打开，不再依赖系统文件关联。
- 启用 Mod 后不再自动弹出旧版手册，改由工具页按需打开。

### 修复

- 修复渲染结束后页面仍显示“正在合成音频”的状态残留。
- 修复帮助按钮在 Steam 叠加界面或系统文件关联不可用时无响应的问题。
- 修复发布工作流在同名分支和标签同时存在时检出错误代码的问题。

### 文档

- 清理用户手册、项目说明和开发文档中的重复版本号，版本信息统一保留在 `CHANGELOG.md` 和 Mod 元数据中。
- 更新 WebUI、渲染锁定、终态提示和发布流程说明。

## 1.4.5

### 新增

- 新增独立 Web 设置页，统一管理功能开关、通用设置、谱面渲染、云同步和工具功能。
- 新增 Steam 叠加界面打开设置页的方式；叠加界面不可用时自动回退到系统默认浏览器。
- 新增完整的谱面渲染进度页面，显示阶段、帧数、速度、重复帧、剩余时间、编码器、内存预算、队列预算和输出路径。

### 优化

- UMM 设置面板精简为 Web 设置页快捷键，默认快捷键为 `Ctrl+Shift+E`。
- 渲染期间允许在 Web 设置页中查看完整进度并直接取消渲染，不再打开游戏内设置浮窗。
- Web 设置页断线后会自动重连，并通过完整状态快照恢复当前页面状态。
- 构建后的工具和第三方依赖统一归档到 `ThirdParty` 目录，减少发布包的目录层级。

### 修复

- 修复渲染进度偶尔停止更新、必须刷新页面才能看到最新进度的问题。
- 修复 SSE 状态数据格式异常导致前端无法接收实时更新的问题。
- 修复取消渲染需要点击两次，以及取消后错误提示“当前没有进行的渲染”的问题。
- 修复浏览器页面连接异常后无法自动恢复的问题。
- 修复设置修改缺少保存成功反馈的问题。

### 文档

- 更新 Web 设置页、渲染进度、Steam 叠加界面打开方式和构建发布说明。

## 1.3.4

### 修复

- 修复 4K、120 FPS 等高规格渲染中，短暂取不到音频就被误判为没有音频，导致任务提前结束的问题。

### 文档

- 更新使用手册和开发文档至 1.3.4，补充高帧率音频恢复说明。

## 1.3.3

### 修复

- 修复谱面重复引用同一张缺失图片时加载中断，导致编辑器一直停留在加载状态的问题。

### 文档

- 更新使用手册和开发文档至 1.3.3，并补充重复缺图加载保护说明。

## 1.3.2

### 新增

- 谱面视频渲染新增「游戏画面渲染（兼容模式）」，可录入额外摄像机、游戏与编辑器界面以及最终屏幕效果。
- 设置面板和渲染浮窗新增画面捕获方式切换；默认继续使用「摄像机渲染（推荐）」。
- 新增公开谱面渲染任务 API，其他 Mod 可以创建渲染任务、读取进度、主动完成或取消渲染并接收最终结果。
- 公共 API 支持渲染器自动播放、重新开始但由外部 Mod 控制播放，以及附着当前播放三种通用模式。
- 公共 API 支持摄像机与游戏画面捕获、整首与指定范围渲染，以及现有视频、音频和编码选项。

### 优化

- 游戏画面渲染会直接跟随当前游戏窗口分辨率，开始渲染后自动隐藏 Editor Tweaks 浮窗，并可按 `Esc` 取消。
- 摄像机渲染继续支持自定义输出分辨率和纯净谱面画面，原有用户升级后无需修改设置。
- 完善两种画面捕获方式的状态提示、渲染摘要、错误信息和诊断记录。
- 外部 Mod 启动的渲染任务可以选择隐藏内置进度界面，同时继续保持渲染输入保护。
- API 渲染请求使用独立配置，不会覆盖玩家保存的 Editor Tweaks 设置。

### 修复

- 修复部分使用额外摄像机、屏幕空间界面或特殊最终画面效果的谱面无法完整渲染的问题。
- 修复游戏画面渲染导出的视频上下颠倒的问题。

### 文档

- 重写用户操作手册，补充压缩包处理、两种渲染模式、功能兼容状态和常见问题说明。
- 更新项目架构、设置、本地化与技术栈文档。
- 新增独立 API 文档目录，补充接口版本、请求字段、播放模式、任务生命周期、错误码、线程规则和调用示例。

## 1.3.1

### 新增

- 新增由 Mod 接管的谱面压缩包处理，支持 ZIP、ADOZIP、RAR、7z、TAR、GZip、BZip2、XZ、CAB，以及 TGZ、TBZ2、TXZ 等常见格式。
- 设置面板新增「旧版 ZIP 文件名编码」选项，支持自动检测或手动指定 CP949、GB18030、Shift-JIS、CP437。
- 功能兼容状态新增「压缩包压缩与解压」，该功能不兼容时不会影响其他功能。

### 优化

- 编辑器打开谱面和 CLS 导入现在可以直接选择常见格式的压缩包；压缩 TAR 会自动完成多层解压。
- 优化旧版 ZIP 的韩文、中文、日文和 DOS 文件名识别，并结合谱面资源引用选择更合适的编码。
- 优化谱面导出，保留图片、音乐、视频等资源的相对目录和 Unicode 文件名；导出的 `.adozip` 仍为原版游戏可读取的标准 ZIP。
- 解压时继续限制条目数量和总大小，并阻止路径穿越、文件覆盖及重复路径。

### 修复

- 修复部分旧谱面压缩包中的韩文、中文或日文文件名显示乱码，导致谱面资源无法加载的问题。
- 修复导出时不同子目录中的同名资源可能发生路径冲突的问题。

## 1.3.0

### 新增

- 设置面板新增游戏版本兼容状态，可查看各功能是否可用，以及因游戏更新失效的功能和简要原因。

### 优化

- 各项功能改为独立加载；游戏更新导致某个功能不兼容时，只会停用对应功能，其他功能仍可正常使用，完整错误信息会记录在日志中。
- 谱面渲染和浮窗输入保护增加依赖检查；缺少必要补丁时会安全禁用对应入口，避免进入不完整的渲染状态。
- 优化带视频背景谱面的离线渲染：视频启动时仅同步一次，并按输出帧率保持连续解码，减少持续校正造成的卡顿、抽帧和画面跳动。

## 1.2.8

### 优化

- 优化离线谱面视频渲染时的视频背景同步，减少视频背景与音乐、谱面时间轴不同步，以及卡顿、重复帧和跳帧的问题；渲染结束或失败后会恢复正常播放状态。
- 改进音频捕获的启动和异常恢复，降低因音频捕获状态异常导致渲染失败的概率。
- 构建时下载的 FFmpeg 更新至 8.1.2，使用更新的编码组件。

### 修复

- 修复 Steam 云设置在新版 Steam 环境下上传或下载可能失败的问题，并改进空云存档的异常处理。
- 修复 FFmpeg 下载完整性检查可能误判的问题，避免损坏的下载包被继续使用。
- 修复音频捕获失败时仍可能继续生成无声视频的问题；现在检测到音频捕获失败会终止本次渲染并显示错误信息。

## 1.2.7

### 新增

- 新增使用手册页面，首次启用 Mod 时会自动打开。
- 设置面板新增「打开使用手册」按钮，可随时打开本地使用说明。
- 渲染分辨率预设新增竖屏 `9:16`，一键设置为 `1080x1920`。
- 渲染分辨率预设新增超宽屏 `21:9`，一键设置为 `2560x1080`。

## 1.2.6

### 优化

- 渲染进度中的重复帧统计不再计入「限制帧率」事件造成的刻意复用帧，避免把谱面效果误显示为导出卡顿。

### 修复

- 修复谱面视频渲染未正确保留「限制帧率」事件效果的问题，导出画面现在会按事件设定的低帧率节奏复用画面。
- 修复「仅渲染选中段落」从砖块 0 开始时可能一直等待并提示准备渲染超时的问题。

## 1.2.5

### 新增

- 设置面板顶部新增 **Steam 云存档同步** 区域，提供「上传设置存档」和「下载设置存档」两个手动按钮，替代此前版本的全自动云同步。

### 修复

- 修复跟随游戏官方版本升级后，自动打击因 `controller.moving` 语义变化而失效的问题。改用 `SetAllPlayerResponsive(true)` 直接控制玩家响应状态。

## 1.2.4

### 新增

- 普通渲染设置新增视频输出格式：MP4、MKV、MOV。
- 新增「专业 FFmpeg 设置」折叠面板，包含自定义 FFmpeg 合成参数字段。
- 新增「打开 FFmpeg 参数参考帮助」按钮，点击打开 `Resources/FFmpegReference.html`。
- 新增 `Resources/FFmpegReference.html`：深色主题全中文 FFmpeg 参数参考文档，涵盖全局选项、视频编码器、码率控制、预设、滤镜、像素格式、音频编码器、容器格式、Mod 管道说明和 18 个实用配方示例。
- 高级设置展开时显示详细安全警告文本，明确提醒非专业用户不要修改。

### 修复

- 修复 `UpdateScreenClamp` Postfix 对 CameraAspect 装饰无条件设置 `clampToScreen = true`，导致部分装饰在播放时一直显示。改为仅在编辑器环境且装饰已启用屏幕裁剪时修正位置，不再强制覆写裁剪状态。

## 1.2.3

### 新增

- 高级渲染设置新增音频格式选项：AAC（有损，默认）、FLAC（无损）、ALAC（无损，MP4 原生支持）。

### 优化

- 优化图表渲染设置界面布局。

## 1.2.2 - 2026-05-29

### 新增

- 普通渲染设置新增分辨率快捷预设：1080p、2K、4K。
- 普通渲染设置新增帧率快捷预设：30、60、120。
- 新增「仅渲染选中段落」开关。开启后可在编辑器中框选连续砖块，只导出该段落。
- 高级渲染设置新增视频码率限制，默认 `0` 表示按当前分辨率和帧率自动推荐。
- 新增离线谱面视频渲染性能档位：自动均衡（默认，优先 NVENC，失败回退 x264）、最快、均衡、质量、CPU 兼容、自定义。
- 新增渲染预览模式：完整预览、暗色预览、极简预览。
- 新增实验性 BGRA GPU readback 模式，默认仍使用稳定 RGBA。
- 渲染进度窗口新增中文性能信息（单帧内存占用、缓存上限、GPU 回读队列上限、编码缓存队列上限）。
- 新增构建和发行脚本：`build-dev.bat`（开发构建）、`build-release.bat`（正式发行，自动递增版本号）。
- 每次构建生成 `Build/<ModId>-<Version>/` 目录和对应 `.zip`。
- 新增可选自动递增版本号：`dotnet build /p:BumpModVersion=true`。

### 优化

- 重构谱面渲染器结构，拆分为 ChartRenderPlaybackController / ChartRenderFramePipeline / ChartRenderMemoryBudget / ChartRenderProgressModel / ChartRenderOptionValues。
- FFmpeg 视频编码改为带目标码率、最大码率和缓冲区限制，减少 4K 视频体积过大和播放器峰值码率卡顿。
- GPU readback pending 数按分辨率动态限制：1080p 最多 8 帧、1440p 最多 6 帧、4K 最多 4 帧、8K 最多 2 帧。
- FFmpeg 写入队列改为按分辨率和内存预算计算，不再固定缓存大量帧。
- 浮窗渲染规格显示实际目标码率。
- 片段渲染限制自动打击终点，避免尾巴录制期间继续打到选区后面的砖块。
- 片段渲染等待官方 checkpoint 切入实际游玩状态后再开始采集，避免录到静音淡入和黑场准备阶段。
- 片段渲染不再使用「结束后延迟停止秒数」，到选区终点即停止。
- 4K/8K 渲染时更保守地控制内存峰值。
- 渲染窗口布局加高加宽，避免取消按钮遮挡状态信息。
- 构建前清空 `out/`，防止历史残留文件混进最终压缩包。

### 修复

- 修复片段渲染时球停在选区起点、音频淡入、进度条反复到 100% 后回退的问题。
- 修复 `UpdateScreenClamp` Postfix 对 CameraAspect 装饰无条件设置 `clampToScreen = true`，导致部分装饰在播放时一直显示。
- 保留并强化谱面从第 0 格稳定开始的保护逻辑。
- 保留视觉时钟锚定后才启用自动播放的保护逻辑，避免开头跳砖块和球抽搐。
- 修复渲染性能信息未翻译、过长且被按钮遮挡的问题。
- 修复构建打包脚本在 Windows 路径末尾反斜杠下参数被吞的问题。

### 文档

- 更新根目录 `README.md` 的渲染、构建、发行说明。
- 新增 `Doc/BuildAndRelease.md`。
- 更新 `Doc/ChartRendering.md`，记录结构拆分、内存预算、队列策略和编码档位。
- 更新 `Doc/SettingsAndLocalization.md`，记录新增设置字段。

## 1.1.0 - 2026-05-28

### 新增

- 新增编辑器内谱面视频渲染浮窗。
- 支持离线定帧导出 MP4。
- 支持 Unity `AudioRenderer` 捕获原曲、打拍音、长按音效和 `PlaySound`。
- 支持隐藏或显示 Perfect / Early / Late 等判定文字。
- 支持谱面结束后额外录制尾巴秒数。
- 支持构建时自动下载并部署 FFmpeg。

### 修复

- 修复从中途播放谱面时视频背景延迟或漂移的问题。
- 修复渲染开局概率性跳到后面砖块的问题。
- 修复渲染期间球抽搐、自动播放追块的问题。
- 修复渲染音频和画面相位偏移的问题。
