# ChartRendering 设置

UMM 面板只录入 Web UI 快捷键，默认 `Ctrl+Shift+E`。渲染设置在 Web UI 中保存：

- 工作区、导出目录、宽高、帧率、CRF 和码率。
- 摄像机/游戏画面来源、回读格式、预览模式。
- 编码档位、音频格式、视频格式和音画偏移。
- 结束尾巴、判定文字、选中段落和自定义 FFmpeg 合成参数。

`Resources/localization.json` 只包含渲染状态和渲染器使用的文本。
